## Imporant Rules

1. Do not push directly to the `main` branch
2. For any changes that you make, create a separate branch, then confirm the changes look good before creating a PR
3. Always add test coverage and run lint and test checks with `npm run lint`, `npm run format`, `npm run prisma:format` and `npm run test:cov`
